// Test LSP with lambdas
func apply(x: int, op: int lambda[int]) -> int {
    return op(x);
}

func main() -> int {
    let doubled = apply(5, int lambda[n: int] {
        return n * 2;
    });
    
    return doubled;
}

